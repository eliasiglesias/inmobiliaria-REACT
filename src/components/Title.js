import React from "react";
import { Row, Col } from "reactstrap";

const Title = () => {
	return (
		<Row className="py-3">
			<Col className="d-flex justify-content-center">
				<h2 className="text-warning">Buscatucasa.es</h2>
			</Col>
		</Row>
	);
};

export default Title;
